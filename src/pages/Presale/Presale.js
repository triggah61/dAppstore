import React from 'react';
import PresaleCountdown from './PresaleCountdown';
import PresaleItems from './PresaleItems';

const Presale = () => (
  <>
    <PresaleCountdown />
    <PresaleItems />
  </>
);

export default Presale;
