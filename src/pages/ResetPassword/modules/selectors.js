export const submitting = state => state.resetPasswordReducer.submitting;
export const messageType = state => state.resetPasswordReducer.messageType;
export const message = state => state.resetPasswordReducer.message;
